import React from 'react'
import MemeGenerator from './MemeGenerator';

function Header(){
    return(
        <header>
            <MemeGenerator />
        </header>
    );
}

export default Header;